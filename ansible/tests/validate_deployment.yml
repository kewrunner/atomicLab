---
- name: Validate Local Documents MCP role in check mode
  hosts: all
  gather_facts: true
  become: false
  roles:
    - role: local_documents_mcp
  vars:
    mcp_target_host: localhost
    mcp_exposed_directory: /tmp
