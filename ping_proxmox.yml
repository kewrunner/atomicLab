---
- name: Validate AWX connectivity to WAZO lab through Proxmox
  hosts: all
  gather_facts: false
  become: false

  tasks:

    - name: Test Ansible connectivity through Proxmox proxy
      ansible.builtin.ping:
